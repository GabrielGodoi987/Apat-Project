import { createTheme } from "@mui/material";

export const ThemeCustomized = createTheme({
    palette:{
        primary: {
            main: '#9F1918'
        },
        secondary: {
            main: '#000'
        }
    }
})